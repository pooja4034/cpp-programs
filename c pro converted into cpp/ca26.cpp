#include<stdio.h>
#include<iostream>
using namespace std;
int main()
{
    int no;

    cout<<"Enter the Number: ";
    cin>>no;

    if(no%5==0&&no%5==0)
        cout<<"No is Divisible by 7 and 5";
    else
        cout<<"No is not Divisible by 7 and 5";
}
